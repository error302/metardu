import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Survey Plan Demo | METARDU',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
