// Layout constants shared by the SVG map renderer (page.tsx Map tab) and
// the standalone SVG export generator (generators.ts).
//
// Extracted from src/app/tools/contour-generator/page.tsx.

export const SVG_WIDTH = 900;
export const SVG_HEIGHT = 650;
export const MARGIN = 60;
